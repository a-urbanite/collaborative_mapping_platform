import React from 'react'
import { useMap } from 'react-leaflet'

const MarkerList = () => {
  const map = useMap()
  return (
    <div>
      <ul>
        {}
      </ul>
    </div>
  )
}

export default MarkerList